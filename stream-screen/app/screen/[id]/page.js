import StreamLayout from "@/components/stream/StreamLayout";

export default function ScreenPage({ params }) {
    // In the future, we can use params.id to fetch specific data or change the look
    return <StreamLayout />;
}
